# ─────────────────────────────────────────────────────────────
#  Contoh konfigurasi environment untuk ORB Live Trading System.
#  Cara pakai:  copy file ini jadi `.env`  →  isi nilainya.
#      cp .env.example .env
#  (yang dibaca pipeline adalah `.env`, bukan `.env.example`)
# ─────────────────────────────────────────────────────────────

# API key Claude — WAJIB untuk slot vision (type: vision).
# Ambil dari https://console.anthropic.com → Settings → API Keys.
# Slot ORB (orb30_nas, orb30_xau) TIDAK butuh ini.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
